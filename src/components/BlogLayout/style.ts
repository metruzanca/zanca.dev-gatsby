import styled from "styled-components";

import {theme} from '../../style'

export const ReadingDiv = styled.div`
  margin: ${theme.spacing_0} auto;
  max-width: ${theme.maxWidth_wrapper};
  padding: ${theme.spacing_10} ${theme.spacing_5};
`